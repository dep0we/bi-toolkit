# Define the metric before querying

A metric definition is the exact rule for calculating a number. Write it before
running the query. If the definition changes after results are visible, record
the reason and rerun the work.
